include("subfolder/library.leek");

say(CHIP_FORTRESS + " " + getChips());

useChip(CHIP_FORTRESS, getLeek());

var enemy = getNearestEnemy();

say("ARMES " + getWeapons());

if (count(getWeapons())) {
	setWeapon(getWeapons()[0]);
}

moveToward(enemy);

useWeapon(enemy);